# pi-tmux-images

Entry point: `extensions/index.ts`. `/image` must remain TUI-only: use custom entries, never Pi message APIs or model image attachments. Keep Kitty cleanup limited to IDs allocated by this runtime.

Run `mise run check`; run `mise run verify` for lifecycle/rendering work.
